1)Создание переменной
Пользователь обращается к библиотеке с целью создания переменной с определенным начальным значением. Возвращается либо указатель на созданный элемент либо null, в случае если создать элемент не удалось.
2)Поиск переменной
Пользователь обращается к библиотеке с целью нахождения конкретного элемента в библиотеке. Возвращается либо указатель на найденный элемент либо null, в случае если найти элемент не удалось.
3)Удаление переменной
Пользователь обращается к библиотеке с целью удаления конкретного элемента из библиотеки. Возвращается либо 0, если элемент найден, либо 1, в остальных случаях.
4)Сохранение и загрузка в/из файл(а)
Пользователь или система обращается к библиотеке с целью сохранения или загрузки библиотеки в/из файл(а). Возвращает 0, если операция выполнилась успешно, или 1, если операция не может быть выполненой.
